import React, { Component } from 'react'
import AnimeData from './AnimePages/AnimeData';

export default class Manga extends Component {
    render() {
        return (
            <div>
                <AnimeData/>
            </div>
        )
    }
}
